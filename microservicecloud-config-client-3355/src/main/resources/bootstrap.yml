spring:
  cloud:
    config:
      name: microservicecloud-config-client   #从github上读取的资源文件 ，注意没有后缀名称
      profile: dev                     #本次访问的配置项
      label: master
      uri: http://config-3344.com:3344 #本微服务启动后先去找3344 服务通过springcloudconfig获取github服务地址